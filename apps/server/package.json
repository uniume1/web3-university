{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.mjs"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.12",
    "@hono/trpc-server": "^0.4.2",
    "@trpc/server": "catalog:",
    "@web3-school/api": "workspace:*",
    "@web3-school/db": "workspace:*",
    "@web3-school/env": "workspace:*",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@web3-school/config": "workspace:*",
    "tsdown": "^0.22.14",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
